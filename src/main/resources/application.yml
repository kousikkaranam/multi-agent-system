server:
  port: 8000

ollama:
  base-url: http://localhost:11434
  model: qwen2.5-coder:14b
  max-tokens: 2048
  temperature: 0.7

spring:
  application:
    name: multi-agent-system

# Web search — get a free key at https://tavily.com (1000 searches/month)
# Without this, DuckDuckGo is used as free fallback (no key needed)
tavily:
  api-key: ${TAVILY_API_KEY:}
